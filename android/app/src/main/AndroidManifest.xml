<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- [جديد - Push Notifications: المرحلة 3.4] أيقونة ولون الإشعار
             الافتراضيين اللي أندرويد بيستخدمهم لأي إشعار FCM بييجي والتطبيق
             مقفول/بالخلفية (لما مفيش notification builder مخصص). الأيقونة
             لازم تبقى silhouette أبيض شفاف بس (ic_stat_notify.xml) -
             أندرويد بيرفض يعرض أيقونة ملوّنة في status bar. اللون من هوية
             سِكّاوي الذهبية (#D4AF37) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <service
            android:name=".StepCounterForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- [جديد - إصلاح باج "عداد الخطوات أقل من Fit"] بيشغّل الخدمة
             تلقائيًا فور إقلاع الجهاز، عشان الـ baseline بتاع
             TYPE_STEP_COUNTER يتسجل قريب من منتصف الليل/الإقلاع مش وقت
             ما المستخدم يفتح التطبيق بإيده -->
        <receiver
            android:name=".BootStepCounterReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- [جديد] لازم على أندرويد 10+ (API 29+) عشان نقدر نوصل لحساس عدّ الخطوات -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- [جديد - إصلاح باج "عداد الخطوات أقل من Fit"] لازم عشان
         BootStepCounterReceiver يقدر يستقبل حدث إقلاع الجهاز ويشغّل
         الخدمة تلقائيًا من غير ما ينتظر المستخدم يفتح التطبيق -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- [جديد] لازم عشان نقدر نشغّل Foreground Service خالص -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- [جديد] لازم على أندرويد 14+ (API 34+) - نوع الخدمة "health" مخصص
         لتطبيقات تتبع اللياقة/الصحة زي عداد الخطوات -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />

    <!-- [جديد] لازم على أندرويد 13+ (API 33+) عشان نقدر نعرض إشعار الخدمة الثابت -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- [إصلاح - باج حقيقي "التطبيق مش بيطلب إذن الموقع"]: كانت
         الصلاحيتين دول مش موجودتين خالص في الملف - عشان كده Capacitor
         Geolocation كان بيفشل فورًا (أو مبيطلعش أي Prompt أصلاً) بدل ما
         يسأل المستخدم، حتى بعد تثبيت بلجن @capacitor/geolocation نفسه.
         ACCESS_COARSE_LOCATION مطلوبة بجانب ACCESS_FINE_LOCATION على
         أندرويد الحديث عشان نافذة الإذن تظهر صح -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- [جديد - متابعة موضوع "الخطوات أقل من Fit" على أجهزة OEM] لازم
         عشان نقدر نطلب من المستخدم استثناء التطبيق من "توفير البطارية"
         عبر ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS (نافذة موافقة
         رسمية من نظام أندرويد نفسه - مش حيلة/API غير موثق). من غير
         الصلاحية دي، الـ Intent هيرمي SecurityException على أي جهاز -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Features: required="false" حتى لا يحجب متجر Google Play التطبيق عن الأجهزة التي لا تحتوي هذه الحساسات -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.stepdetector" android:required="false" />

    <!-- [جديد - Autostart] شاشات "التشغيل التلقائي" الخاصة بشركات
         شاومي/هواوي/هونر/أوبو/فيفو/ميزو..إلخ بنفتحها بـ Intent صريح
         (component name محدد) في openAutostartSettings()، فمش محتاجين
         <queries> أصلاً تقنيًا (قيود ظهور الحزم في أندرويد 11+ بتخص
         بس استعلامات PackageManager الضمنية زي queryIntentActivities،
         مش startActivity بـ component صريح). بنسيبها هنا موثّقة/فاضية
         عمدًا كتنويه، ومعتمدين على try/catch (ActivityNotFoundException/
         SecurityException) جوه الـ Plugin نفسه للتعامل مع أي ROM
         مايكونش فيه الـ Activity دي أصلاً أو غيّر اسمها -->
</manifest>